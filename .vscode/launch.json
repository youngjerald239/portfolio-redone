{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Chrome against localhost",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src"
    }
  ],
  "compounds": [
    {
      "name": "Start and Debug App",
      "configurations": ["Debug Chrome against localhost"],
      "preLaunchTask": "Start React App"
    }
  ]
}
